import React from 'react';

class Mainpage extends React.Component { //"component" make user using react.component for class
  render (){
      return(
          <div>
              this is Main page using docker
          </div>
      );
  }
}

export default Mainpage;